import { PasswordChangeForm } from "@/components/PasswordChangeForm";

export const metadata = { title: "Password · KCS Teacher Evaluation" };

export default function PasswordPage() {
  return <PasswordChangeForm/>;
}
